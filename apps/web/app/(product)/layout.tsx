import './product.css';

/** Everything signed-in lives under this group: the dashboard, account pages, and projects. */
export default function ProductLayout({ children }: { children: React.ReactNode }) {
  return children;
}
